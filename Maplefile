# Maplefile for AMReX

# Name of the base image
base="ubuntu"

# Name of the default image
image="amrex"

# Name of the local container
container="amrex"

# Container backend
backend="docker"

# Default commands for execute
execute=["./configure --dim=1 --prefix=/home/lib/amrex/1D && make install",
         "./configure --dim=2 --prefix=/home/lib/amrex/2D && make install",
         "./configure --dim=3 --prefix=/home/lib/amrex/3D && make install",
         "make clean && make distclean"]
